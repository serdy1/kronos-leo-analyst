# Core
fastapi==0.115.6
# uvicorn[standard] is intentionally NOT used here.
# The [standard] extra pulls in httptools and brotli, both of which cause
# automatic response compression that breaks SSE streaming through Cloudflare
# and Render proxies.  We use the minimal uvicorn build instead and add only
# uvloop (for performance) and h11 (for correct chunked-flush behaviour).
uvicorn==0.34.0
uvloop==0.21.0
h11==0.14.0
pydantic==2.10.4
python-dotenv==1.0.1

# Data & Finance
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.54

# Utils
httpx>=0.27.0
